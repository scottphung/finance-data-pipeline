version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"

  app:
    build: .
    ports:
      - "8501:8501"

  db:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234